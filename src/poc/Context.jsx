import React from "react";
import { createContext } from "react";
import { useContext } from "react";
const contextWrapper = createContext(); // create a store
function Context() {
  return <div></div>;
}

export default Context;
